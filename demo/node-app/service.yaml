apiVersion: v1
kind: Service
metadata:
  name: demo-node
  namespace: demo
  labels:
    app: demo-node
spec:
  selector:
    app: demo-node
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
