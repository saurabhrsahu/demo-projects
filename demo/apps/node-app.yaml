apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-node
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/microshare/argo.git
    targetRevision: HEAD
    path: demo/node-app
  destination:
    server: https://kubernetes.default.svc
    namespace: demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
