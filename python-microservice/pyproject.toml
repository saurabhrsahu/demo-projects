[project]
name = "python-microservice"
version = "0.1.0"
description = "Small FastAPI microservice for deployment testing"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[project.scripts]
python-microservice = "app.main:run"

